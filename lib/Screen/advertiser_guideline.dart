import 'package:flutter/material.dart';

class AdvertiserGuideline extends StatelessWidget {
  const AdvertiserGuideline({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Advertiser Guideline'),
      ),
    );
  }
}